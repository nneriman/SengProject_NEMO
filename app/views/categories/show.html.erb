<%= render :partial => "common/header"%>
<div style="text-align: center; margin-top: 30px;">
     <%= render partial: 'categories/photo', collection: @category.photos %>
    <br><br> <%= link_to 'Back', categories_path %>

</div>
